% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotations.R
\name{fetchAnnotations}
\alias{fetchAnnotations}
\title{Fetch ENSEMBL ids, ENTREZ ids and gene names using bioMart}
\usage{
fetchAnnotations(species = c("hs", "mm"), ensembl_version = NULL)
}
\arguments{
\item{species}{Species identifiers (only "hs" or "mm" are supported).}

\item{ensembl_version}{Version of the ensembl annotation to use,
passed to \code{biomaRt::useEnsembl}.
The default \code{NULL} uses the current annotation release.}
}
\value{
A data.frame of three columns:
\code{c("ensembl_id", "entrez_id", "gene_name")}
}
\description{
Fetches annotations for human ("hs") or mouse ("mm") from the
Ensembl BioMart.
}
\details{
Only human "hs" and mouse "mm" are supported.
Annotation must contain the columns \code{"entrez_id"} and
\code{"gene_name"}.
}
\examples{
\dontrun{
ann_hs <- fetchAnnotations(species="hs")
ann_mm <- fetchAnnotations(species="mm")
}
}
